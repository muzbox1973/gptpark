{
    "name": "gpt-park-blog",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "db:init": "wrangler d1 execute gpt-park-db --file=./schema.sql"
    },
    "dependencies": {
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}